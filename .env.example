# =================================
# Docker Heatmap Environment Config
# =================================

# Environment
ENVIRONMENT=development

# Database
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_NAME=docker_heatmap
DATABASE_URL=postgres://postgres:your-secure-password-here@localhost:5432/docker_heatmap?sslmode=disable

# GitHub OAuth
# Create OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:8080/api/auth/github/callback

# JWT Secret (generate a secure random string)
# Example: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Encryption Key (MUST be exactly 32 characters for AES-256)
# Example: openssl rand -hex 16
ENCRYPTION_KEY=your-32-char-encryption-key!!!!

# Frontend URL
FRONTEND_URL=http://localhost:3000

# API URL (for frontend)
NEXT_PUBLIC_API_URL=http://localhost:8080/api
